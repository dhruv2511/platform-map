apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: platform-map

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

images:
  - name: platform-map
    newName: us-docker.pkg.dev/your-gcp-project/your-artifact-repo/platform-map
    newTag: latest