apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: platform-map
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: platform-map.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: platform-map
                port:
                  number: 80